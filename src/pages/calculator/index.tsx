import { Grid, Typography } from '@mui/material';
import DollarCalculator from '../../components/kit/calculator';

export default function Calculator() {
	return (
		<Grid container>
			<DollarCalculator />
		</Grid>
	);
}
